# Copie para .env e preencha só o que for usar.
# Não commite o arquivo .env. Não cole chaves no chat.

# Cloudflare R2 (DVC). Conta criada; preencha depois de bucket + API token.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_ENDPOINT_URL=https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# DVC_REMOTE_URL=s3://bets-cerveja-landing
